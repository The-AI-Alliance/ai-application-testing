[project]
name = "running-open-webui"
version = "0.1.0"
description = "Open WebUI setup for use with the ai_application_testing project."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "greenlet>=3.4.0",
    "open-webui>=0.6.23",
]
