# README for the `src/tools` Directory

This directory contains the command-line tools described in the user guide for generating and validating synthetic data, etc. They are not to be confused with "tools" in the sense used for agents and MCP (model context protocol).

For examples of agent tools, see `src/apps/chatbot/skills`.
